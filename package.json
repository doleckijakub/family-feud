{
  "name": "family-feud",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "node --import=ts-node/register src/server.ts",
    "build": "tsc && npm run build:public",
    "build:public": "esbuild src/public/*.ts --bundle --format=esm --outdir=public"
  },
  "dependencies": {
    "express": "^4.19.0",
    "ws": "^8.15.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/ws": "^8.5.10",
    "esbuild": "^0.24.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  }
}
